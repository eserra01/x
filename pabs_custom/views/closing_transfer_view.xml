<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <record id="closing_transfer_management_view" model="ir.ui.view">
    <field name="name">closing.transfer.management.view</field>
    <field name="model">closing.transfer.registry</field>
    <field name="arch" type="xml">
      <tree>
        <field name="name"/>
        <field name="picking_id"/>
        <field name="warehouse_id"/>
        <field name="date"/>
      </tree>
    </field>
  </record>

  <record id="closing_transfer_management_action" model="ir.actions.act_window">
    <field name="name">Administración de cortes</field>
    <field name="res_model">closing.transfer.registry</field>
    <field name="view_mode">tree</field>
  </record>

</odoo>
